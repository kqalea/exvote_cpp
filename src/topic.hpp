#include<vector>
#include<stdint.h>
#include<string>

using namespace std;
class Topic {
public:
    vector<int32_t> twoSum(vector<int32_t>& numbers, int target);
    bool isUniqueChars(string str);
};
